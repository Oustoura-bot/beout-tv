# إصلاح مشكلة عدم ظهور رابط التحميل في صفحة المقال

## وصف المشكلة

كان رابط التحميل (`download_url`) وكود التحميل (`download_code`) يظهران بشكل صحيح في لوحة التحكم بعد الحفظ، لكنهما لا يظهران في صفحة المقال العامة (`/article/[slug]`).

## التشخيص بعد تتبع دورة البيانات كاملة

| المرحلة | الحالة |
|---------|--------|
| الحفظ في لوحة التحكم (`ArticleForm` → `actions.ts`) | سليمة: الحقلان يُرسلان ويُحفظان |
| جلب المقال (`getArticleBySlug` — `select("*")`) | سليمة: تجلب كل الأعمدة |
| عرض صفحة المقال (`{article.download_url && ...}`) | سليمة: الشرط صحيح |
| مخطط قاعدة البيانات (`supabase/schema.sql` + `FIX_DATABASE.sql`) | **الخلل هنا**: جدول `articles` لا يحتوي على عمودي `download_url` و`download_code` |

بما أن الكود سليم بالكامل، فإن القيم التي يُدخلها المحرّر في لوحة التحكم **لا تُحفظ فعليًا في قاعدة البيانات** لأن العمودين غير موجودين في مخطط جدول `articles`. قد يظهر الجدول في لوحة التحكم بشكل طبيعي لأن Supabase يتجاهل الأعمدة غير الموجودة، لكن عند القراءة تعود القيمة `null` ولا يظهر قسم التحميل في صفحة المقال.

## خطوات الإصلاح

### 1. تشغيل سكريبت إصلاح قاعدة البيانات (إلزامي — مرة واحدة)

افتح [Supabase SQL Editor](https://app.supabase.com) وشغّل محتوى الملف:

```
supabase/add_download_columns.sql
```

هذا السكريبت يضيف العمودين المفقودين إلى جدول `articles` ويعيد تحميل ذاكرة التخزين المؤقت للمخطط (Schema Cache) عبر `NOTIFY pgrst, 'reload schema'`. تم أيضًا تحديث `supabase/FIX_DATABASE.sql` ليشمل العمودين.

### 2. إعادة النشر في Vercel

ادفع التغييرات (تم تلقائيًا عبر هذا الالتزام) ثم أعد نشر الموقع في Vercel مع تفعيل خيار **"Clear build cache"** للتأكد من قراءة المخطط الجديد.

### 3. إعادة إدخال روابط التحميل للمقالات الحالية

الروابط التي أُدخلت سابقًا في لوحة التحكم **لم تُحفظ في قاعدة البيانات** لأن الأعمدة لم تكن موجودة. يجب فتح كل مقال في لوحة التحكم وإعادة إدخال رابط التحميل ثم الضغط على حفظ. بعد ذلك ستظهر روابط التحميل في صفحات المقالات فورًا.

## تحسينات الكود المضافة في هذا الإصلاح

- `actions.ts`: رسائل خطأ واضحة عند اكتشاف أعمدة مفقودة أو خطأ Schema Cache، توجه المحرّر مباشرة إلى السكريبت المطلوب.
- `actions.ts`: إعادة التحقق من القيم بعد الحفظ (إرجاع `download_url`/`download_code` في استعلام التأكيد) مع تسجيل تحذير إذا فُقدت القيم.
- `actions.ts`: إعادة تحديث مسار صفحة المقال (`revalidatePath`) بعد التعديل لضمان ظهور التغيير فورًا.

## كيفية التحقق من نجاح الإصلاح

1. بعد تشغيل السكريبت، شغّل في SQL Editor:
   ```sql
   SELECT column_name FROM information_schema.columns
   WHERE table_name = 'articles' ORDER BY ordinal_position;
   ```
   يجب أن تظهر `download_url` و`download_code` في القائمة.
2. افتح أي مقال في لوحة التحكم، أدخل رابط تحميل، واحفظ.
3. افتح المقال في الواجهة العامة: يجب أن يظهر قسم "تحميل المحتوى / الملف" مع الزر وكود التحميل إن وُجد.
